server:
  port: 0
#  servlet:
#    context-path: /mock

spring:
  application:
    name: MOCK-SERVICE

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8089/eureka
  instance:
    hostname: localhost
    instance-id: ${spring.application.name}:${instanceId:${random.value}}